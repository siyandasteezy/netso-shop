[build]
  base    = "."
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"

# Required for Next.js on Netlify (App Router, API routes, middleware)
[[plugins]]
  package = "@netlify/plugin-nextjs"
